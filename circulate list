def circulate_list(lst, steps):
    steps %= len(lst) 
    return lst[-steps:] + lst[:-steps]

my_list = [1, 2, 3, 4, 5]
circulated_list = circulate_list(my_list, 2)
print("Original list:", my_list)
print("Circulated list:", circulated_list)

